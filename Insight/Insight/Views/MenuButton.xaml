<UserControl x:Class="Insight.Views.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Insight.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="{DynamicResource TertiaryBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image x:Name="imgIcon" Margin="20, 20, 20, 10" Source="/Insight;component/Resources/trophy.png"></Image>
        <TextBlock x:Name="txtTitle" Grid.Row="1" VerticalAlignment="Center" Foreground="White" FontSize="18" HorizontalAlignment="Center" Margin="10" FontFamily="pack://application:,,,/Fonts/#Abel">Title</TextBlock>
        
    </Grid>
</UserControl>
